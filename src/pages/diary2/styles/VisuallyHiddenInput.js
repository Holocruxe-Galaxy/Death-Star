import { styled } from '@mui/material';

export const VisuallyHiddenInput = styled('input')({
  // clip: 'rect(0 0 0 0)',
  // clipPath: 'inset(50%)',
  // width: 5,
  // height: 5,
  // overflow: 'hidden',
  // whiteSpace: 'nowrap',
});
